It uses the SDL2 library to render graphics 
and uses CMake as a build system.

#Ubuntu
sudo apt install git build-essential pkg-config cmake cmake-data libsdl2-dev libsdl2-gfx-dev

mkdir build
cd build
cmake ..
make